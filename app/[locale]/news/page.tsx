import { NewsList } from "@/components/news/news-list";import { getPublishedArticles } from "@/lib/supabase/articles";export const revalidate=60;export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const articles=await getPublishedArticles(locale);return <main><section className="page-hero dark"><div className="container"><span className="eyebrow">News & Insights</span><h1>Ideas for products that make occasions brighter.</h1><p>Explore product, customization, retail, and celebration-sourcing perspectives from PARTY HUB.</p></div></section><section className="section light"><div className="container"><NewsList locale={locale} articles={articles}/></div></section></main>}
