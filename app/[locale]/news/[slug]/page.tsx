import { notFound } from "next/navigation";import { ArticleDetail } from "@/components/news/article-detail";import { getArticleBySlug } from "@/lib/supabase/articles";export const revalidate=60;export const dynamicParams=true;export default async function Page({params}:{params:Promise<{locale:string;slug:string}>}){const {locale,slug}=await params;const article=await getArticleBySlug(slug,locale);if(!article)notFound();return <ArticleDetail article={article}/>}
