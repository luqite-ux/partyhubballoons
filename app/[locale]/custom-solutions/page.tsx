import { InfoPage } from "@/components/layout/info-page";import { publicPages } from "@/content/pages";import {buildStaticPageMetadata} from "@/lib/seo/metadata";export const metadata=buildStaticPageMetadata("custom-solutions");export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;return <InfoPage locale={locale} page={publicPages["custom-solutions"]}/>}
