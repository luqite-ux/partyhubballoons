import Link from "next/link";import { ArrowUpRight } from "lucide-react";import type { Article } from "@/lib/supabase/articles";export function NewsList({locale,articles}:{locale:string;articles:Article[]}){if(!articles.length)return <div className="empty-state"><span className="eyebrow">News & Insights</span><h2>Insights are being prepared.</h2><p>New product, customization, and sourcing articles will appear here after publication.</p></div>;return <div className="news-grid">{articles.map(a=><Link aria-label={a.title} href={`/${locale}/news/${a.slug}`} key={a.slug}><span>{a.publishedAt??"Insight"}</span><h2>{a.title}</h2><p>{a.excerpt}</p><b>Read article <ArrowUpRight size={16}/></b></Link>)}</div>}
